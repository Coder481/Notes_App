<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NotesApp" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_theme_color</item>
        <item name="colorSecondary">@color/secondary_theme_color</item>
        <item name="android:statusBarColor">@color/secondary_theme_color</item>
        <item name="fontFamily">@font/sourcesans3_regular</item>
    </style>

    <style name="Theme.NotesApp" parent="Base.Theme.NotesApp" />
</resources>